<Window x:Class="CaloriesCalculator.AddFoodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adaugă Aliment în Meniu" Height="400" Width="500" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Caută:"/>
            <TextBox Name="txtSearch" Width="200" Margin="5,0" TextChanged="txtSearch_TextChanged"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Name="gridFoods" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Denumire" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Calorii" Binding="{Binding Calories}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
            <Label Content="Cantitate (g):"/>
            <TextBox Name="txtQuantity" Width="100" Margin="5,0" Text="100"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnAdd" Content="Adaugă" Width="75" Margin="5" Click="btnAdd_Click"/>
            <Button Name="btnCancel" Content="Anulează" Width="75" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
